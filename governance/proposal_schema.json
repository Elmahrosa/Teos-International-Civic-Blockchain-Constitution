{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TEOS Governance Proposal",
  "description": "Schema defining a constitution-compliant governance proposal within the TEOS Civic Blockchain Ecosystem.",
  "type": "object",
  "required": ["proposer", "badge", "proposal", "type"],
  "properties": {
    "proposer": {
      "type": "string",
      "description": "Wallet or decentralized identifier of the proposal author.",
      "minLength": 32
    },
    "badge": {
      "type": "string",
      "description": "Civic role badge verified before proposal acceptance.",
      "enum": ["Citizen", "Validator", "Founder"]
    },
    "proposal": {
      "type": "string",
      "description": "Human-readable proposal text describing the motion or action.",
      "minLength": 10
    },
    "type": {
      "type": "string",
      "description": "Governance proposal category determining voting and execution requirements.",
      "enum": ["upgrade", "treasury", "amendment"]
    }
  },
  "additionalProperties": false
}
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Governance Proposal",
  "type": "object",
  "properties": {
    "proposer": { "type": "string", "minLength": 1 },
    "badge": { "type": "string", "minLength": 1 },
    "proposal": { "type": "string", "minLength": 1 },
    "type": { "type": "string", "enum": ["upgrade", "vote", "policy", "amendment"] }
  },
  "required": ["proposer", "badge", "proposal", "type"],
  "additionalProperties": false
}

